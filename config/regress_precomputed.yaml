detector: straw

regressor:
  deep-set:
    features: [[64, 128], [128, 96]]
    p_dropout:

optimizer:
  adabelief:
    learning_rate: 1e-4

initial_design: data/design/default.json

batch: 10
epochs: 1000
steps: 1000

validation_batches: 8

report: output/losses2.png

# Directory containing precomputed chunk files
# Generated by: python scripts/save_all_data.py
precomputed_dir: precomputed_data3
